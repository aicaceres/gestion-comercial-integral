<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="application/vnd.ms-excel; charset=UTF-8" />
   </head>
   <body  >  
<style>
table{ font-family:'Arial';font-size: 12px; }
table tr th{
    border-top: 1px solid gray;
    border-left: 1px solid gray;
    background: #CCCCCC;
    font-size: 13px;
    }
table tr td{
    border: 1px solid black;
    height: 20px;
    vertical-align: middle;
    padding: 5px;
}
.align-right{
    text-align: right;
    padding-right: 10px !important;
}
</style>       
      <h2 text-align="center"> Listado de Precios - Lista: {{ filtro[0]|upper }} </h2>                      
        <h4><strong>Rubro:</strong> {{ filtro[2] }} - <strong>Proveedor:</strong> {{ filtro[1] }} </h4>        
        <div height="10px"></div>
        {% if search %}
            <h4> Término de Búsqueda: "{{search}}" </h4>            
        {%endif%}       
        <table cellspacing="0">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Código</th>
                    <th>Producto</th>
                    {% if filtro[1]=='Todos'%}
                    <th>Proveedor</th>
                    {% endif%}                    
                    {% if filtro[2]=='Todos'%}
                    <th>Rubro</th>
                    {% endif%}                 
                    <th>Costo</th>                
                    <th>Precio</th>                
                    <th>Ült.Modif.</th>                          
                </tr>                        
            </thead>
            <tbody>
                {% for item in items %}               
                    <tr>
                        <td class="align-right">{{loop.index}}</td>
                        <td>{{item.producto.codigo}}</td>
                        <td>{{item.producto.nombre}}</td>
                         {% if filtro[1]=='Todos'%}
                        <td>{{item.producto.proveedor.nombre}}</td>
                        {% endif%}
                        {% if filtro[2]=='Todos'%}
                        <td>{{item.producto.rubro.nombre}}</td>
                        {% endif%}
                        <td class="align-right">{{item.producto.costo}}</td>                                    
                        <td class="align-right">{{item.precio}}</td>                                    
                        <td>{{ item.updated|date('d-m-Y H:i:s') }} </td>                                    
                    </tr>                           
                {% endfor %}
            </tbody>              
        </table>    

   </body>
</html>