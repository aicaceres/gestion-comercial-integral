{% extends "AppBundle::layout.html.twig" %}
{% block pageheader %}
<div class="pageheader notab">
    <h1 class="pagetitle">Administración de Ventas</h1>       
</div><!--pageheader-->
{% endblock %}
{% block contentwrapper %}
{% include "AppBundle::notificacion.html.twig" %}
   
<div id="contentwrapper" class="contentwrapper">
<div>      
    <form id="searchform" action="{{ path('ventas_venta') }}" method="get">        
        <div style="display: inline-block; margin-bottom: 10px;">
            &nbsp;&nbsp;&nbsp;
            <label><strong>Punto de Venta:</strong></label>
            <select class="uniformselect" id="selectPtoVta" name="puntoventaId">
                <option value="0">Todos</option>
            {% for pto in puntos %}
                <option value="{{pto.id}}" {% if pto.id==puntoventaId %} selected="selected" {% endif %}> {{pto.nombre}}</option>
            {% endfor %}
            </select>  
        </div>    
        <div style="display: inline-block; margin-right: 30px;">     
            &nbsp;&nbsp;&nbsp;
            <label><strong>Fecha Venta: </strong></label>
            <input type="text" class="width100 datepicker" title="Fecha desde" value="{{desde}}" name="desde" id="desde" /> 
            <input type="text" class="width100 datepicker" title="Fecha hasta" value="{{hasta}}" name="hasta" id="hasta" /> 
            <a href="javascript:void(0)" class="editar btn btnaction btn_search" onClick="jQuery('#searchform').submit();" title="Buscar"></a>
        </div>        
    </form>
</div><!-- button -->     
    <table cellpadding="0" cellspacing="0" border="0" class="stdtable" id="dyntable2">
        <thead>
            <tr>
                <th>Punto Venta</th>
                <th># Operación</th>
                <th>Fecha</th>
                <th>Cliente</th>
                <th>Forma de Pago</th>
                <th>Total</th>
                <th>Estado</th>
                <th class="nosort actionbutton addbutton">
                   {% if app.user.access(app.session.get('unidneg_id'),'ventas_venta_new') %}
                    <a class="editar create" href="{{ path('ventas_venta_new') }}">Nueva Venta</a>
                    {% else %} &nbsp; {% endif %} 
                </th>
            </tr>
        </thead>
        <tbody>
           {% for entity in entities %}
            <tr>
                <td>{{entity.puntoVenta|upper }}</td>
                <td>{{entity.nroOperacion}}</td>
                <td><span class="hidden">{{ entity.fechaVenta|date('YmdHi') }}</span>{{ entity.fechaVenta|date('d-m-Y H:i') }}</td>
                <td>{{ entity.cliente }}</td>
                <td>{{ entity.formaPago }}</td>
                <td class="alignright">{{ entity.getTotal|number_format(3,'.',',') }}</td>                
                <td class="estado">{{ entity.estado }}</td>
                <td class="buttons">                    
                     <a href="{{ path('ventas_venta_show', { 'id': entity.id }) }}" class="editar btn btnaction btn_folder" title="Ver Venta"></a>
                    {% if app.user.access(app.session.get('unidneg_id'),'ventas_factura_cancel') %}
                    <a style="display:none" href="javascript:void(0)" url="{{ path('ventas_factura_cancel', { 'id': entity.id }) }}" title="Cancelar"
                       class="cancelFactura btn btnaction btn_trash"></a>
                    {% endif %}
                </td>
            </tr>
           {% endfor %}
        </tbody>
    </table>     
</div>
 {% endblock %}
{% block extra_javascripts %}
{% include "AppBundle::block-list-js.html.twig" %}
<script type="text/javascript">
jQuery(document).ready(function($){
    $('#selectPtoVta').change(function(){ 
        $('#searchform').submit();
    }); 
    $('.datepicker').datepicker({dateFormat: 'dd-mm-yy'});
    
    var oTable = $('#dyntable2').dataTable();
    oTable.fnSort( [ [1,'desc'],[0,'desc'] ] );
    
   $('#printform').on('submit',function(){
        $('#clienteid').val($('#selectCliente').val());
        $('#fdesde').val($('#desde').val());
        $('#fhasta').val($('#hasta').val());
        $('#searchterm').val( $('#dyntable2_filter input').val() );
        var datos = [];
        oTable.api().rows( { search:'applied' } ).data().each(function(value, index) {  
            var i = value[1].indexOf('</span>');
            if( i >-1 ){
                var fecha = value[1].substr(i+7);
                value[1]=fecha;
            }
            value.splice(6);
            datos[index] = value;
        }); 
        $('#datalist').val(JSON.stringify(datos));
    });   
   
   /*$('#dyntable2 tr td.estado').each(function(){
       checkButtons($(this));
   });*/
  function checkButtons(estado){
      txt = estado.html();
      buttons = estado.parent().find('.buttons');
      buttons.children('.btnaction').hide();
      if(txt=='FACTURADO' || txt=='ANULADO'){ buttons.find('.btn_folder').show();   }
      else { buttons.find('.btn_pencil, .btn_trash').show();}
  };      
});
</script>        
{% endblock %}