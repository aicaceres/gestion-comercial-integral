{% extends "AppBundle::layout.html.twig" %}
{% block pageheader %}
<div class="pageheader notab">
    <h1 class="pagetitle">Administración de Actividad Comercial</h1>
    <span class="pagedesc">Listado de actividad comercial del sistema</span>
</div><!--pageheader-->
{% endblock %}
{% block contentwrapper %}
    {% include "AppBundle::notificacion.html.twig" %}

<div id="contentwrapper" class="contentwrapper">
    <table cellpadding="0" cellspacing="0" border="0" class="stdtable" id="dyntable2">
        <thead>
            <tr>
                <th>Código</th>                
                <th>Nombre</th>                
                <th>Exento</th>                
                <th>Inscripto</th>                
                <th>No Inscripto</th>                
                <th>Mínimo</th>                
                <th class="nosort actionbutton addbutton">
                    {% if app.user.access(app.session.get('unidneg_id'),'sistema_actividadcomercial_new') %}
                    <a class="editar create" href="{{ path('sistema_actividadcomercial_new') }}">Agregar Actividad</a>
                    {% else %} &nbsp; {% endif %}
                </th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.codigo|upper }}</td>                
                <td>{{ entity.nombre|upper }}</td>                
                <td class="alignright">{{ entity.exento}}</td>                
                <td class="alignright">{{ entity.inscripto }}</td>                
                <td class="alignright">{{ entity.noInscripto }}</td>                
                <td class="alignright">{{ entity.minimo }}</td>                
                <td>
                     {% if app.user.access(app.session.get('unidneg_id'),'sistema_actividadcomercial_edit') %}
                    <a href="{{ path('sistema_actividadcomercial_edit', { 'id': entity.id }) }}" class="editar btn btnaction btn_pencil" ></a>&nbsp;
                    {% endif %}
                    {% if app.user.access(app.session.get('unidneg_id'),'sistema_actividadcomercial_delete') %}
                        <a href url="{{ path('sistema_actividadcomercial_delete', { 'id': entity.id }) }}" class="delete btn btnaction btn_trash" title="Borrar"></a>                                                               
                    {% endif %}
                </td>
            </tr>
           {% endfor %}           
        </tbody>
    </table> 
</div>
{% endblock %}
{% block extra_javascripts %}
{% include "AppBundle::block-list-js.html.twig" %}
{% endblock %}